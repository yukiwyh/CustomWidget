<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wyh.customwidget" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoadingActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".MyTopBarActivity" />
        <activity android:name=".EmptyListViewActivity" />
        <activity android:name=".HideToolBarListViewActivity" />
        <activity android:name=".MoveableViewActivity" />
        <activity android:name=".SideSlipActivity" />
        <activity android:name=".SlipActivity" />
        <activity android:name=".BannerActivity"/>
    </application>

</manifest>
